<!DOCTYPE html>
<html lang="en">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Green Bees - Todo List</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" integrity="sha384-1q8mTJOASx8j1Au+a5WDVnPi2lkFfwwEAa8hDDdjZlpLegxhjVME1fgjWPGmkzs7" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div class="container">
      <div class="jumbotron">
        <h1>TodoList</h1>
        <h3>Green Bees</h3>
      </div>

      <div id="taskSubmit" class="row">
        <div class="col-sm-6">
          <form>
            <div class="form-group">
              <label for="newTaskInput">Add a new task</label>
              <input type="text" class="form-control" id="newTaskInput" placeholder="Task..." v-model="taskDesc">
            </div>
            <button class="btn btn-success" v-on:click="addTask">Add</button>
          </form>
        </div>
      </div>

      <br>

      <div id="taskList" class="row">
        <div class="col-sm-12">
          <h5>Tasks</h5>
          <ul class="list-group">
            <template v-for="task in tasks">
              <li class="list-group-item">
                <div class="vertical-align">
                  <div class="flex-half">
                    <h4 class="list-group-item-heading">{{ task.task }}</h4>
                    <p class="list-group-item-text">Importance: {{ task.importance }} | Completed: {{ task.completed }}</p>
                  </div>
                  <div class="flex-half">
                    <span class="glyphicon glyphicon-ok btn-complete" v-on:click="removeTask($index)"></span>
                  </div>
                </div>
              </li>
            </template>
          </ul>
        </div>
        <div class="col-sm-12">
          <button class="btn btn-info btn-block" v-on:click="getTasks">Reload</button>
        </div>
      </div>

      <br>
      <hr>

      <div class="row">
        <div class="col-sm-12">
          <form> <!-- Remove after newTask is working -->
            <input />
            <button type="submit">Submit</button>
          </form>
          <ul class="nav nav-tabs"> <!-- Bootstrap tabs -->
            <li class="active"><a href="#">To-do</a></li>
            <li><a href="#">Work</a></li>
            <li><a href="#">+Add</a></li>
          </ul>
          <div id="app">
            <newTask></newTask> <!-- Not working -->
            <tasks :list="tasks"></tasks>
          </div>

          <template id="newTask-template"> <!-- New task form -->
            <form>
              <input v-model="task"/>
              <button type="submit">Submit</button>
            </form>
          </template>

          <template id="tasks-template"> <!-- Renders List of Tasks -->
            <div>
              <ul class="list-unstyled">
                <li :class="{ 'completed': task.completed }"
                    v-for="task in list">
                    <input type="checkbox"/>
                    {{ task.task }}
                    <span class="caretToggle"></span>
                    <strong @click="deleteTask(task)">X</strong>
                </li>
              </ul>
            </div>
          </template>

          <template id="task-template"> <!-- Not done. Component for individual task? -->
            <span></span>
          </template>
        </div>
      </div>

    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.2.4/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js" integrity="sha384-0mSbJDEHialfmuBBQP6A4Qrprq5OVfW37PRR3j5ELqxss1yVqOtnepnHVP9aJ7xS" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/vue/1.0.26/vue.js"></script>
    <script src="/javascripts/api.js"></script>
    <script src="/javascripts/main.js"></script>
  </body>
</html>
