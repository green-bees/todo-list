'use strict';

var database = {};

database.createItem = function(item, callback) {
};

database.readItem = function(id, callback) {
};

database.updateItem = function(id, item, callback) {
};

database.deleteItem = function(id, callback) {
};

module.exports = database;
